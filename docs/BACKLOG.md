# Backlog

Ideas and requests that are agreed on but not scheduled yet.

- **Mouse wheel on sliders/scrollable lists.** The scroll wheel should work
  wherever there is a slider or scrollbar: turning the wheel over a scrollable
  list (e.g. the options screen's settings list) scrolls it, and over a slider
  row (gamma, particle density, volumes) it should adjust the slider value.
  Today the wheel does nothing there. (Requested 2026-08-11.)
